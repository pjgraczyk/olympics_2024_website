{
    "name": "olympic_predictions_2024",
    "private": true,
    "version": "0.0.1",
    "scripts": {
        "start:frontend": "cd frontend && npm run dev",
        "start:backend": "conda activate olympics_website && cd backend && uvicorn app.main:app --reload",
        "dev": "concurrently \"npm run start:frontend\" \"npm run start:backend\""
    },
    "devDependencies": {
        "concurrently": "^6.2.0"
    }
}
